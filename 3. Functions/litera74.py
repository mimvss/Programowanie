# --- Plik: letters.py ---

def count_letter(text, letter):
    # Funkcja .count() to najprostszy sposób w Pythonie, 
    # żeby policzyć ile razy coś występuje w tekście.
    return text.count(letter)