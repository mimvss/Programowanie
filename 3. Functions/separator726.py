# --- Plik: separator.py ---

def f(text):
    # Funkcja "-".join(text) bierze każdą literę z 'text'
    # i skleja je, wstawiając myślnik pomiędzy nie.
    wynik = "-".join(text)
    
    return wynik